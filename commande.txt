sudo lsof -i :5432
sudo kill 1234

gpt-context-builder

dans backend
flask --app run.py run --debug --port=5001

dans projet_nusuk
cloudflared tunnel --url http://localhost:5001

une fois qu'on a l'url du tunnel on mets à jour les variables d'env sur l'interface vercel puis on déploie et on met à jour le webhook dans stripe en y insérant l'url jusqua .com et en laissant la fin qui existe déjà api/stripe etc




1 terminal
API 
(venv) ➜  backend docker run -d -p 6379:6379 redis
(venv) ➜  backend celery -A run.celery worker --loglevel=info -P gevent

2 terminal
(venv) ➜  backend # Assurez-vous que votre venv est activé
flask --app run.py run --debug --port=5001


3terminal
front :
(venv) ➜  frontend-next git:(main) ✗ npm run dev



nusuk_documents@proton.me    pour supabase


stripe login
stripe listen --forward-to http://localhost:5001/api/webhooks/stripe


redis://red-d3t0k3i4d50c73enn86g:6379